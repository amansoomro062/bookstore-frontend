import { NgModule } from '@angular/core';
import { FormsModule } from '@angular/forms';

const CostomeComponent = [   
   FormsModule,
]

@NgModule({
  imports: [CostomeComponent],
  exports: [CostomeComponent]
})

export class CustomeformModule { }
